# Entry point — simply delegates to the composition root in main.py.
# In Ports & Adapters this file has no architectural role; it just
# lets you run the project with `python -m HexagonalArchitecture`.
from main import main

main()
